import { PlaceholderPage } from "@/components/domain/PlaceholderPage";

export default function RoadmapPage() {
  return (
    <PlaceholderPage
      title="Roadmap"
      description="Màn hình Roadmap chưa có reference trong package, nên chưa mở rộng thành workflow mới."
    />
  );
}
